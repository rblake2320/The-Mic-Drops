# The MIC Drops — API vhost
# File: /etc/nginx/sites-available/mic-drops
#
# Replace MIC_DROPS_DOMAIN below with your actual domain (e.g. api.micdrop.app)
# Temporary: use srv1740069.hstgr.cloud (no SSL needed for initial test)

# ── Temp: direct via VPS hostname (no SSL) ───────────────────────────────────
# Uncomment this block for initial smoke-test, then switch to SSL block below
#
# server {
#     listen 80;
#     server_name srv1740069.hstgr.cloud;
#
#     location /api/ {
#         proxy_pass         http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header   Host $host;
#         proxy_set_header   X-Real-IP $remote_addr;
#         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         proxy_read_timeout 60s;
#     }
# }

# ── Production: custom domain with SSL ──────────────────────────────────────
server {
    listen 80;
    server_name MIC_DROPS_DOMAIN;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name MIC_DROPS_DOMAIN;

    ssl_certificate     /etc/letsencrypt/live/MIC_DROPS_DOMAIN/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/MIC_DROPS_DOMAIN/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    client_max_body_size 10m;

    location /api/ {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }
}
