name = "the-mic-drops"
pages_build_output_dir = "dist"

# Build happens in GitHub Actions (npm run build with VITE_API_URL secret),
# then `wrangler pages deploy dist` uploads the result.
# VITE_API_URL is baked in at build time — current: https://srv1740069.hstgr.cloud
